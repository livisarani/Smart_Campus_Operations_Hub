# Optional frontend OAuth override
# Leave empty in most cases; Vite proxy already forwards /oauth2 to backend.
VITE_OAUTH_BASE_URL=http://localhost:8081

# Optional API base for direct calls when proxy is not used
# VITE_API_BASE_URL=http://localhost:8081
